# TODO

## Features

- [ ] **Open mic mode** — Continuous listening mode that doesn't require holding
  the hotkey. Voice activity detection (VAD) triggers recording automatically;
  silence ends the segment and sends it to transcription. Should be togglable
  from the tray menu alongside the existing push-to-talk hotkey mode.
